#pragma once

#include <string>

//============================================================

class Player
{

//============================================================

	public:
		Player();

		std::string getName();
		void setName(std::string name);
		char getToken();
		void setToken(char token);

//============================================================

	private:
		std::string _name;
		char _token;

//============================================================

	protected:

//============================================================

};

//============================================================